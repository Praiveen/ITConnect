# #
# # Build stage
# #
# FROM gradle:latest AS build
# COPY --chown=gradle:gradle . /home/gradle/src
# WORKDIR /home/gradle/src
# RUN gradle build

# #
# # Package stage
# #
# FROM openjdk:21
# WORKDIR /app
# COPY --from=build /home/gradle/src/build/libs/*.jar /app/demo.jar
# EXPOSE 8080
# ENTRYPOINT ["java","-jar","demo.jar"]

FROM eclipse-temurin:21-jdk
WORKDIR /app
COPY . .
RUN chmod +x ./gradlew
RUN ./gradlew build --no-daemon
EXPOSE 8080
CMD ["java", "-jar", "build/libs/CorporateEventer-0.0.1-SNAPSHOT.jar"]



FROM gradle:8.6.0-jdk21-alpine AS build
COPY --chown=gradle:gradle . /home/gradle/src
WORKDIR /home/gradle/src
RUN gradle clean build


#
# Package stage
#
FROM openjdk:21
WORKDIR /app
COPY --from=build /home/gradle/src/build/libs/*.jar /app/demo.jar
EXPOSE 8080
ENTRYPOINT ["java","-jar","demo.jar"]








env

#
# Build stage
#
FROM gradle:latest AS build
COPY --chown=gradle:gradle . /home/gradle/src
WORKDIR /home/gradle/src
RUN gradle build

#
# Package stage
#
FROM openjdk:21
WORKDIR /app
COPY --from=build /home/gradle/src/build/libs/*.jar /app/demo.jar
EXPOSE 8080

# Добавляем поддержку переменных окружения
ENV DATABASE_URL=''
ENV DATABASE_USERNAME=''
ENV DATABASE_PASSWORD=''
ENV JWT_SECRET_KEY=''

ENTRYPOINT ["java", "-jar", "demo.jar"]